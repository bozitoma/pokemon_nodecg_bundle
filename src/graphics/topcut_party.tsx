import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './topcut_party/App.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
